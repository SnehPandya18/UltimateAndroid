<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.snehpandya.ultimateandroid">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity
            android:name=".ImageActivity"
            android:label="@string/app_name"
            android:parentActivityName=".WidgetsActivity"/>
        <activity
            android:name=".WidgetsActivity"
            android:label="@string/widgets"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".WebViewActivity"
            android:label="WebView"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".NetworkActivity"
            android:label="@string/network"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".ListViewsActivity"
            android:label="@string/listview"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".ListViewActivity"
            android:label="@string/listview"
            android:parentActivityName=".ListViewsActivity"/>
        <activity
            android:name=".CustomListView"
            android:label="@string/custom_listview"
            android:parentActivityName=".ListViewsActivity"/>
        <activity
            android:name=".BatteryActivity"
            android:label="@string/battery_status"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>